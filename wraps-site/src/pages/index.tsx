import React from 'react';

import App from '@/components/App';
import SEO from '@/components/seo'

export default function ProjectOverview() {
  return (
    <main>
      <App />
      <SEO title="Personal Wraps Clollection" description="Personal collection of wrap files and patch archives for use with meson build" />
    </main>
  );
}
